import React, { useState } from "react";
import Layout from "../components/Layout";
import { Typography } from "@mui/material";

const Admin = () => {
  return (
    <Layout>
      <Typography variant="h4">전체 차량 목록</Typography>
    </Layout>
  );
};

export default Admin;
